import { type TeamMembership } from '../schema';

export async function getPendingMemberships(teamId: number): Promise<TeamMembership[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all pending membership requests for a specific team.
    // Used by team members to see who wants to join their team and approve/reject requests.
    return Promise.resolve([]);
}